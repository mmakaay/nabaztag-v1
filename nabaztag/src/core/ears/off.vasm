import core/constants

$motor     equ R0
$direction equ R1

@Core.Ears.Off()
    push R0 R1
    ld $direction %STOP
    ld $motor %MOTOR_LEFT 
    motor $motor $direction
    ld $motor %MOTOR_RIGHT 
    motor $motor $direction
    wait 1
    pull last
    rts
